# I

`i` marks italic content inside formatting containers such as `p`.

## Mark a Term

```xml
<p>This mode is called <i>streaming</i>.</p>
```

The final glyph style depends on CSS and the available font.
