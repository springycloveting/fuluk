# Text

The `text` component is used to display text. It is similar to the `span` or `p` element in HTML.

## Usage

```xml
<text class="title">Hello World</text>
```

## Features

- Supports a variety of style settings such as font size, color, and font weight.
- Supports text wrapping and alignment.
