# P

`p` lays out text and inline semantic components as a paragraph.

## Combine Inline Content

```xml
<p>AIUI supports <b>emphasis</b> and <snippet>inlineCode()</snippet>.</p>
```

It supports plain text plus children such as `b`, `i`, inline `snippet`, and inline `formula`, while inheriting CSS text styles.
