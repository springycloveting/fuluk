# Blockquote

`blockquote` presents a standalone quotation and supports text and inline semantic children.

## Display a Quotation

```xml
<blockquote style="border-left: 3px solid #999; padding-left: 12px;">Keep interface content concise.</blockquote>
```
