# List

`list` contains `list-item` children. Use CSS to control direction, spacing, and marker styling.

## Display a List of Items

```xml
<list style="display: flex; flex-direction: column; gap: 8px;">
  <list-item>Prepare data</list-item>
  <list-item>Generate the interface</list-item>
</list>
```
