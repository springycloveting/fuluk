# Fuluk Gateway Web 界面使用手册（一步一步）

本手册根据 [USAGE.md](USAGE.md) 与 Web 界面实际元素编写，按步骤带你在浏览器中完成：登录 → 配置 → 创建会话 → 查看筛选 → 交互 → 管理 → 自然语言助手 → 眼镜配对。

> 界面按钮名称以页面实际显示为准（中文/英文界面均可）。所有步骤的默认地址为 `http://127.0.0.1:8787`。

## 第 0 步 · 开始前检查

1. 确认网关已启动：

   ```bash
   curl http://127.0.0.1:8787/health
   ```

   预期返回 `{"ok": true, "tmux": true}`（若 tmux 未安装，返回中 `tmux` 为 false）。

2. 在浏览器打开 `http://127.0.0.1:8787`，页面标题为「AI CLI Session Manager」。
3. 准备好启动网关时设置的 `SESSION_GATEWAY_TOKEN`（生产环境为强随机值，请勿使用默认示例值）。

## 第 1 步 · 登录（配置认证令牌）

1. 点击顶部右侧的「配置」按钮，打开配置窗口。
2. 在「Bearer token」输入框中填入 `SESSION_GATEWAY_TOKEN`（输入框为密码类型，不会明文显示）。
3. 点击「保存」。
4. 预期：窗口关闭，之后页面所有 API 请求自动携带该 Token；Token 保存在浏览器本地，下次打开无需重新填写。

> 认证失败时页面请求会返回 401，请回到本步核对 Token 是否与启动时一致。

## 第 2 步 · 熟悉主界面布局

| 区域 | 位置 | 内容 |
|---|---|---|
| 顶部栏 | 页面上方 | 左侧：当前选中会话标题（默认「未选择会话」）；右侧：「需要确认」「已完成」通知按钮（有通知时出现）、「All Yes」、「历史」、「新建」、「配置」、会话抽屉按钮（箭头图标） |
| 终端区 | 页面中部 | 输出区域（xterm 终端）、快捷键行（`+` 可添加快捷键）、底部输入框（占位「发送到当前会话」）、助手按钮（星形图标）、「发送」按钮 |
| 会话抽屉 | 页面右侧 | 「刷新」、「状态筛选」下拉、「搜索」框（搜索名称/项目/路径）、会话列表 |

## 第 3 步 · 创建会话

1. 点击顶部「新建」按钮，打开「新建会话」窗口。
2. 填写以下字段：

   | 字段 | 说明 | 是否必填 |
   |---|---|---|
   | 类型 | `codex` / `claude` / `opencode` / `pi-os` / `runtime` | 必选 |
   | 会话名 | 例如 `codex-app`；留空则自动生成 | 可选 |
   | 工作目录 | 例如 `/workspace/app`；留空使用默认目录 | 可选 |
   | 项目名 | 自由文本标签，用于后续过滤 | 可选 |

3. 点击「新建」按钮。
4. 预期：会话出现在右侧列表，主终端自动切换到新会话。

> 注意：创建 `codex` / `claude` / `opencode` / `pi-os` 会话时会检查对应 CLI 命令是否存在，未安装会创建失败（`runtime` 类型对应 `/bin/bash`，不受此限制）。

## 第 4 步 · 查看与筛选会话

1. 点击顶部右侧的会话抽屉按钮（箭头图标）打开会话列表。
2. 点击「刷新」更新会话状态（running / stopped / missing）。
3. 使用「状态筛选」下拉切换视图：
   - 未关闭（默认）／全部会话／进行中／已停止／已关闭
4. 在搜索框输入关键字，可按**名称 / 项目 / 路径**过滤会话。
5. 每个会话项显示：名称、相对时间、任务状态（进行中 / 已停止 / 已关闭等）、类型标签（codex/claude…）、工作目录。

## 第 5 步 · 与会话交互

1. **选中会话**：点击列表中的会话项（会高亮），主终端显示该会话的实时输出。
2. **发送指令**：在底部输入框输入文本，点击「发送」或按回车键发送到当前会话。
3. **处理确认请求**：当会话任务状态变化时顶部会出现通知按钮：
   - 「已完成」→ 点击后跳转到对应会话查看结果；
   - 「需要确认」→ 点击后跳转到该会话，可手动发送确认内容。
4. **All Yes**：点击顶部「All Yes」按钮循环切换三种模式：`off` → `session`（仅当前会话自动应答确认）→ `global`（全部会话自动应答确认）。按钮文字显示当前模式。
5. **快捷键**：点击终端区快捷键行右侧的 `+`，填写「显示名称」（如 `Y`）与「值」（如 `yes`），保存后点击该按钮即向当前会话发送该文本并回车。
6. **历史**：点击顶部「历史」查看之前的输入记录。

## 第 6 步 · 停止 / 重启 / 删除会话

在会话列表中，每个会话项右侧有操作按钮，随状态变化：

- **停止**：会话进行中（active）时显示「停止」，点击后停止任务；
- **重启**：会话已停止（stopped / closed）时显示「重启」，点击后重新启动会话；
- **删除**：任何状态下都有「删除」按钮，点击后弹出确认框（正在运行的会话会先停止），确认后删除会话。

> 删除操作不可恢复，确认框中会显示会话名称，请核对后再点「删除」。

## 第 7 步 · 使用自然语言助手（web-pi）

### 7.1 启用并配置助手

1. 打开「配置」窗口，在「助手与命令解析」区域：
   - 勾选「启用 web-pi AI 助手」；
   - （可选）勾选「规则解析失败时用 AI 回退解析」；
   - 填写「API base URL」（OpenAI 兼容端点，如 `http://127.0.0.1:8000/v1`）、「provider:model-id」（如 `openai:gemma-4-E4B-it`）、「API key」（本地无鉴权可填任意值）。
2. 点击「保存」。也可在服务端直接写 `data/session-gateway-settings.json`（见安装指南 2.5 节）。

### 7.2 使用助手

1. 点击底部输入框右侧的**助手按钮（星形图标）**，打开「助手」面板（副标题 web-pi）。
2. 在输入框（占位「查看并总结当前会话。」）输入自然语言命令，点击「发送」。
3. 结果显示在助手面板中；**只有查看输出 / 切换会话这类操作会同步更新主终端**，其余操作（创建、发送、停止、重启等）结果只显示在面板内。

### 7.3 命令示例

| 命令示例 | 效果 |
|---|---|
| `新建一个 codex 会话，目录 /workspace/app。` | 创建新会话 |
| `把这句话发给 claude-main：查看。` | 向指定会话发送文本 |
| `codex-1 最近输出。` | 获取指定会话输出 |
| `列出所有运行中的会话。` | 列出运行中会话 |
| `列出项目 demo 的会话。` | 按项目标签过滤 |
| `停止 opencode-test。` | 停止会话 |
| `重启 session-name。` | 重启会话 |
| `进入 session-name。` | 切换到指定会话 |

### 7.4 同音字智能识别

输入含错别同音字也能正确理解，例如：`绘画列表` → `会话列表`、`新建一个绘画` → `新建一个会话`、`列出所有绘话` → `列出所有会话`。已支持 `绘画/绘话/回话/汇话/会画 → 会话`、`烈表 → 列表`、`烈出 → 列出`、`插询/差询 → 查询`、`床建/创贱 → 创建`、`庭止/亭止 → 停止`、`虫启/冲启 → 重启`、`法送/发诵 → 发送`、`经入/近入 → 进入`、`且换/窃换 → 切换` 等映射。

## 第 8 步 · 眼镜配对（可选）

1. 打开「配置」窗口，找到「AI 眼镜认证」区域。
2. 眼镜端（Rokid AIUI）首次语音输入网关地址后，此处会出现待批准列表。
3. 在待批准列表中点击「批准」或「拒绝」。
4. 批准后眼镜出现在「已授权眼镜」列表中，之后眼镜端无需再输入 token。

## 第 9 步 · 项目标签与过滤

- 创建会话时在「项目名」填写自由文本标签（如 `demo`）；
- 之后用会话抽屉的搜索框输入标签关键字，或用自然语言 `列出项目 demo 的会话。` 过滤；
- 标签只是纯文本标记，不创建群组或共享消息通道。

---

## 常见问题速查

| 问题 | 处理 |
|---|---|
| 打开页面后所有请求 401 | 第 1 步重新配置 Token，确保与启动时 `SESSION_GATEWAY_TOKEN` 一致 |
| 创建 codex/claude 会话失败 | 对应 CLI 未安装，先安装并确认 `codex --version` / `claude --version` 可用 |
| 发送文本后只换行不执行 | 提交键默认 `Enter`；某些 Codex 组合需改用 `C-m` 启动：`SESSION_GATEWAY_CODEX_SUBMIT_KEY=C-m npm run dev` |
| 会话状态不更新 | 点击会话抽屉「刷新」；列表展示的是刷新时刻的状态 |
| 助手面板无响应 | 检查配置中「启用 web-pi AI 助手」已勾选、base URL 可达（`curl <baseUrl>/models`）、模型名正确 |
| tmux 相关错误 | 确认宿主机已安装 tmux：`sudo apt install tmux` |

## 附录 · 界面按钮速查

| 按钮 | 作用 |
|---|---|
| 新建 | 打开创建会话窗口 |
| 配置 | 语言 / 主题 / Token / 助手与命令解析 / AI 眼镜认证 |
| 会话（箭头图标） | 打开 / 关闭会话抽屉 |
| 刷新 | 更新会话列表状态 |
| All Yes | 循环切换 off → session → global 自动确认模式 |
| 历史 | 查看输入历史 |
| 发送 | 向当前会话发送输入框内容 |
| 助手（星形图标） | 打开 web-pi 自然语言助手面板 |
| + | 添加自定义快捷键 |
